# Test and development dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Kept separate from runtime requirements so production deploys stay slim.

-r requirements.txt

# Test runner
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Test DB driver — SQLite in-memory backend for the default conftest fixture.
# When TEST_DATABASE_URL points at Postgres, asyncpg (already in requirements)
# is used instead.
aiosqlite==0.20.0

# Lint / format
ruff==0.8.4
mypy==1.13.0
